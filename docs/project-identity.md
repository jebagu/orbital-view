# Project Identity

## Head Project

```text
Orbital View 1.0
```

`Orbital View` is the current canonical project name and the head line for this repository.

The Swift package name is `OrbitalView`. Swift targets and public symbols keep their existing `OrbitalView*` names for source compatibility.

## Non-Head Variations

These names are historical, transitional, or variant labels. They should not be used as the current product/project name.

| Label | Status | Notes |
| --- | --- | --- |
| Orbital View Kit | non-head | Original scaffold/package identity; retained only where historical docs or module-boundary notes need that context. |
| Orbital View VU Kit | non-head | Earlier VU-focused variant and distribution label; do not use for the current head project. |
| Orbital View Turbo | non-head | Local checkout/worktree label; not a product name. |
| orbital-view-with-objects | non-head | Sibling worktree/branch label; not the head project identity. |
| Orbital View VU Kit Native SceneKit Geodesic Viewport Review App With Preserved Control Rail | non-head | Old verbose review-app bundle label; use `Orbital View 1.0` for current release status. |

## Naming Rules

- Use `Orbital View` for current user-facing project copy.
- Use `Orbital View 1.0` for this pushed checkpoint/release identity.
- Keep `OrbitalView*` target, type, and product names unless a future compatibility-breaking task explicitly renames source symbols.
- Keep historical labels in older changelog entries only when they describe past artifacts.
