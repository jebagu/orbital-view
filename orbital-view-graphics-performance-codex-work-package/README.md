# Orbital View Graphics Performance Codex Work Package

This zip contains a Codex-ready work package for moving Orbital View Kit toward:

```text
Active viewport / interaction target: 120 FPS
Displayed meter update target: 30 FPS
```

The main file is:

```text
orbital-view-graphics-performance-codex-work-package.md
```

Use it as the implementation prompt for Codex. It intentionally starts with diagnostics, counters, and source-level test harnesses before changing cadence, materials, scheduling, or renderer behavior.

The companion file is:

```text
codex-execution-prompt.txt
```

That file is a shorter paste-ready wrapper that tells Codex to execute the work package.
