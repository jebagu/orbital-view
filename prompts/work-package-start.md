# Work Package Start Prompt

```text
We are starting a large work package.

Do not write code yet.

Create or update:

work-packages/[package-name]/MV.md
work-packages/[package-name]/slices/

First decide:

1. Should this use main tree or a new worktree?
2. What is the goal?
3. What is out of scope?
4. What OpenSpec change does this map to, if any?
5. What protected paths are involved?
6. What audio constraints matter?
7. What are the slices?
8. Which slices require specialty review?
9. Which slices can run in parallel?
10. What should Architect monitor?

Return the proposed MV file and slice list.
```
