# Slice 004: Orbital Viewport Visual Mockup

## Status

```text
complete
```

## Goal

Preview the intended OrbitalViewKit monitor viewport in disposable HTML/CSS/JS before writing renderer code.

## Scope

Do:

- Create `mockups/orbital-view-viewport/index.html`.
- Create `mockups/orbital-view-viewport/notes.md`.
- Show center-locked orbit, camera presets, toggles, fake meter glow, and speaker selection.

Do not:

- write Swift renderer or SwiftUI code
- implement real audio processing or real meter capture
- depend on external web libraries
- import DomeLab code

## Protected Path Check

This slice:

```text
does not
```

touch a protected path.

## Verification

```text
mockups/orbital-view-viewport/index.html exists
mockups/orbital-view-viewport/notes.md exists
inline JavaScript parses
```
